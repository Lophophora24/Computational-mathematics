#ifndef PARAMETRS_H
#define PARAMETRS_H

#define h 0.006     // шаг по координате
#define t 0.002     // шаг по времени

//----------Константы в краев. условии------------//

#define alpha 0.9   // коэффициент теплоотдачи
#define sigma 5.67

//------------------------------------------------//

#define a 0.5       // внешний радиус цилиндра
#define b 0.2       // внутренний радиус цилиндра

#define T 1000      // конец временного промежутка (начало в 0)

//--------------Константы в уравнении-------------//

#define c1 920.     // c в области 0 < r < b  (теплопроводность)
#define c2 800      // с в области b < r < a

#define ro1 2000.   // плотность
#define ro2 1500.

#define k1 100      // температуропроводность
#define k2 200

#define q1 0        // поток тепла
#define q2 0

#define TT 20       // температура окружающей среды

#define SET_PARAMS(i) \
    if (x[i] < b) {   \
        c[i] = c1;    \
        ro[i] = ro1;  \
        k[i] = k1;    \
        q[i] = q1;    \
    } else {          \
        c[i] = c2;    \
        ro[i] = ro2;  \
        k[i] = k2;    \
        q[i] = q2;    \
    }

//------------------------------------------------//

#define N a/h     // число ячеек            

#define SIZE_X (int)(N + 2)          // размер массива координат
#define SIZE_T (int)((T - 0.) / t)   // размер массива времени

#endif
